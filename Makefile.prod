#-------------------------------- Project --------------------------------------------------#
SHELL := /bin/bash
include Makefile
export
#-------------------------------- Project --------------------------------------------------#

all:
	@ if [ ! -f .prod ]; then touch .prod; fi
